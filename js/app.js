let son = +prompt("son kiriting  ");
for (let i = 1; i <= son; i++) {
  if (i % 2 === 0) {
    console.log(`${i}`);
  }
}

//let = +prompt(` xoxlagan soningizni kiriting`);

let summa = 0;

for (let i = 0; i <= son; i++) {
  summa = summa + i;
}
alert(summa);
